<ul>
  <% if Admin::AgentsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_agents_url
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_agent_url,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::AgentsController.sortable? and Agent.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Agents"),
                 admin_agents_url,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Agents"),
                 admin_agents_url,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>